# Copy to .env.docker and fill in. .env.docker is gitignored via .dockerignore.

# HTTP server
PORT=8787
PARSEABLE_MCP_ALLOW_PRIVATE=false

# Each HTTP MCP client supplies X-Parseable-URL and X-API-Key headers.
